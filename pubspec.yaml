name: flutter_nhom2
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.9.0
  lottie: ^3.1.2          # Animation developer đẹp
  url_launcher: ^6.3.0    # Mở link GitHub

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    # - assets/lottie/developer.json   # COMMENT DÒNG NÀY ĐI ĐỂ HẾT LỖI VÀNG (nếu chưa có file)